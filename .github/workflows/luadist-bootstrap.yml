name: cooljeanius/LuaDist-bootstrap
on:
  push:
    branches:
    - "**/*"
    - master
  pull_request:
jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
    - name: checkout
      uses: actions/checkout@v4.0.0
    - run: "./bootstrap"
    - run: "./_install/bin/luadist list"
#     # This item has no matching transformer
#     - email:
#         on_success: change
#         on_failure: always
